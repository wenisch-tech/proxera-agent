{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "proxera-agent.serviceAccountName" . }}
  labels:
    app.kubernetes.io/name: {{ include "proxera-agent.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end -}}
