{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "proxera-client.serviceAccountName" . }}
  labels:
    app.kubernetes.io/name: {{ include "proxera-client.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
{{- end -}}
